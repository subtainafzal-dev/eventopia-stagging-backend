const express = require("express");
const router = express.Router();

// Note: All ticket type management routes for promoters have been moved to promoters.routes.js
// to maintain proper API structure: /api/promoter/events/:eventId/ticket-types

module.exports = router;